[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mardas-md2pdf"
version = "1.0.1"
description = "Beautiful Persian/English Markdown to PDF converter with RTL/LTR typography, hierarchical TOC, code highlighting, tables, GUI workflow, and MathJax."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "mragetsars", email = "mragetsars@yahoo.com" }
]
keywords = ["markdown", "pdf", "rtl", "persian", "mathjax", "pygments", "playwright", "gui"]
dependencies = [
  "markdown-it-py>=3.0.0",
  "pygments>=2.17.0",
  "beautifulsoup4>=4.12.0",
  "playwright>=1.45.0",
  "PyYAML>=6.0.0",
  "pypdf>=4.0.0"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "ruff>=0.6.0"
]

[project.scripts]
mrs-md2pdf = "mardas_md2pdf.cli:main"
mrs-md2pdf-gui = "mardas_md2pdf.gui:main"

[tool.setuptools]
packages = ["mardas_md2pdf"]
package-dir = {"mardas_md2pdf" = "src"}
include-package-data = true

[tool.setuptools.package-data]
mardas_md2pdf = [
  "assets/*.css",
  "assets/*.png",
  "assets/*.html",
  "assets/mathjax/*"
]

[tool.ruff]
line-length = 100
src = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
